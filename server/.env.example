# ------------------------------------------------------------------
# KadaHub API configuration
# Copy to `.env` and fill in real values. NEVER commit `.env`.
# ------------------------------------------------------------------

# Server
NODE_ENV=development
PORT=4000

# PostgreSQL (Supabase) connection string.
# Supabase: Project Settings -> Database -> Connection string (URI).
# Use the "Session" pooler URI for the app and keep `sslmode=require`.
# Example:
#   postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:password@localhost:5432/kadahub

# Auth / JWT
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=15m            # session length; matches 15-min inactivity logout
BCRYPT_ROUNDS=10

# CORS: comma-separated list of allowed origins (the Vite dev server)
CLIENT_ORIGIN=http://127.0.0.1:5173,http://localhost:5173

# File storage ("local" now; "s3" later via the storage service interface)
STORAGE_DRIVER=local
UPLOAD_DIR=uploads

# Payment gateway ("mock" now; "stripe" later via the payment service interface)
PAYMENT_DRIVER=mock
STRIPE_SECRET_KEY=
STRIPE_CURRENCY=usd

# AI Virtual Try-On ("mock" now; a real provider later via the tryOn service interface)
TRYON_DRIVER=mock

# Notifications ("console" now; an email/SMS provider later)
NOTIFICATION_DRIVER=console
